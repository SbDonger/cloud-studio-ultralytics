# Ultralytics YOLO11n-P2 + EMA (Neck all detection outputs, with residual)
# 4-scale detection: P2, P3, P4, P5
# EMA applied after each detection layer output in the Neck
# Reference: https://blog.csdn.net/FL1623863129/article/details/148352598

nc: 10

scales:
  n: [0.50, 0.25, 1024]
  s: [0.50, 0.50, 1024]
  m: [0.50, 1.00, 512]
  l: [1.00, 1.00, 512]
  x: [1.00, 1.50, 512]

# YOLO11n backbone (same as standard P2 model)
backbone:
  # [from, repeats, module, args]
  - [-1, 1, Conv, [64, 3, 2]]             # 0-P1/2
  - [-1, 1, Conv, [128, 3, 2]]            # 1-P2/4
  - [-1, 2, C3k2, [256, False, 0.25]]     # 2
  - [-1, 1, Conv, [256, 3, 2]]            # 3-P3/8
  - [-1, 2, C3k2, [512, False, 0.25]]     # 4
  - [-1, 1, Conv, [512, 3, 2]]            # 5-P4/16
  - [-1, 2, C3k2, [512, True]]            # 6
  - [-1, 1, Conv, [1024, 3, 2]]           # 7-P5/32
  - [-1, 2, C3k2, [1024, True]]           # 8
  - [-1, 1, SPPF, [1024, 5]]              # 9
  - [-1, 2, C2PSA, [1024]]                # 10

# YOLO11n head + EMA at each detection output
head:
  # FPN: P5 -> P4
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]
  - [[-1, 6], 1, Concat, [1]]             # cat backbone P4
  - [-1, 2, C3k2, [512, False]]           # 13

  # FPN: P4 -> P3
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]
  - [[-1, 4], 1, Concat, [1]]             # cat backbone P3
  - [-1, 2, C3k2, [256, False]]           # 16

  # FPN: P3 -> P2
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]
  - [[-1, 2], 1, Concat, [1]]             # cat backbone P2
  - [-1, 2, C3k2, [128, False]]           # 19 (P2/4)
  - [-1, 1, EMA, [8]]                     # 20-P2/4 EMA

  # PAN: P2 -> P3
  - [-1, 1, Conv, [128, 3, 2]]
  - [[-1, 16], 1, Concat, [1]]            # cat head P3
  - [-1, 2, C3k2, [256, False]]           # 23 (P3/8)
  - [-1, 1, EMA, [8]]                     # 24-P3/8 EMA

  # PAN: P3 -> P4
  - [-1, 1, Conv, [256, 3, 2]]
  - [[-1, 13], 1, Concat, [1]]            # cat head P4
  - [-1, 2, C3k2, [512, False]]           # 27 (P4/16)
  - [-1, 1, EMA, [8]]                     # 28-P4/16 EMA

  # PAN: P4 -> P5
  - [-1, 1, Conv, [512, 3, 2]]
  - [[-1, 10], 1, Concat, [1]]            # cat head P5
  - [-1, 2, C3k2, [1024, True]]           # 31 (P5/32)

  # Detect (4-scale, EMA on P2/P3/P4)
  - [[20, 24, 28, 31], 1, Detect, [nc]]   # Detect(P2+EMA, P3+EMA, P4+EMA, P5)
